/**
 * fixtures/index.ts
 *
 * Single import point for all custom fixtures.
 * Tests should import from here, not individual fixture files.
 *
 *   import { test, expect } from '../fixtures';
 */

export { test, expect } from './page.fixture';
